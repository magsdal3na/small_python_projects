def add_setting(dict, tuple):
    og_settings_dict = {}
    lower_settings_dict = {key.lower(): k for v, k in og_settings_dict.keys()}
    add_setting.lower(value())
    if key in add_setting():
        print(f"Setting '{key}' already exists! Cannot add a new setting with this name.")
    else:
        add_setting.append()
        print(f"Setting '{key}' added with value '{value}' successfully!")

def update_setting(dict, tuple):
    pass

def delete_setting(dict, key):
    pass

def view_settings(dict):
    pass

test_settings = {}
